{
  "name": "@uixvisor/tokens",
  "version": "0.0.0",
  "private": true,
  "description": "Semantic design tokens (foundation/semantic/component/motion/elevation)",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo \"ERROR: @uixvisor/tokens is not implemented\" && exit 1",
    "lint": "echo \"ERROR: lint is not configured for @uixvisor/tokens\" && exit 1",
    "test": "echo \"ERROR: tests are not configured for @uixvisor/tokens\" && exit 1",
    "typecheck": "echo \"ERROR: typecheck is not configured for @uixvisor/tokens\" && exit 1"
  }
}
