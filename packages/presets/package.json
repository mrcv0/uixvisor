{
  "name": "@uixvisor/presets",
  "version": "0.0.0",
  "private": true,
  "description": "Theme presets (Default, Fintech, ...) built on @uixvisor/tokens",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@uixvisor/tokens": "*"
  },
  "scripts": {
    "build": "echo \"ERROR: @uixvisor/presets is not implemented\" && exit 1",
    "lint": "eslint src",
    "test": "echo \"ERROR: tests are not configured for @uixvisor/presets\" && exit 1",
    "typecheck": "echo \"ERROR: typecheck is not configured for @uixvisor/presets\" && exit 1"
  }
}
