{
  "name": "uixvisor",
  "version": "0.0.0",
  "private": true,
  "description": "UIXVISOR CLI - init/add/list/diff/doctor",
  "license": "MIT",
  "type": "module",
  "bin": {
    "uixvisor": "./dist/index.js"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo \"TODO: build cli\" && exit 0",
    "dev": "echo \"TODO: dev cli\" && exit 0",
    "lint": "echo \"TODO: lint cli\" && exit 0",
    "test": "echo \"TODO: test cli\" && exit 0",
    "typecheck": "tsc --noEmit"
  }
}
