{
  "name": "uixvisor",
  "version": "0.0.0",
  "private": true,
  "description": "UIXVISOR CLI - init/add/list/diff/doctor",
  "license": "MIT",
  "type": "module",
  "bin": {
    "uixvisor": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "echo \"TODO: dev cli\" && exit 0",
    "lint": "echo \"TODO: lint cli\" && exit 0",
    "test": "npm run build && node --test dist/resolve-dependencies.test.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@uixvisor/registry-schema": "0.0.0",
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
