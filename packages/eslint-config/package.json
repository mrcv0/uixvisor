{
  "name": "@uixvisor/eslint-config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared ESLint configuration for UIXVISOR packages and apps",
  "license": "MIT",
  "type": "module",
  "main": "./index.js",
  "scripts": {
    "lint": "echo \"ERROR: lint is not configured for @uixvisor/eslint-config\" && exit 1"
  }
}
