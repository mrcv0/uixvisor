{
  "name": "@uixvisor/registry",
  "version": "0.0.0",
  "private": true,
  "description": "Registry resolution: dependency graph, fetch, checksum verification",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@uixvisor/registry-schema": "*"
  },
  "scripts": {
    "build": "echo \"ERROR: @uixvisor/registry is not implemented\" && exit 1",
    "lint": "echo \"ERROR: lint is not configured for @uixvisor/registry\" && exit 1",
    "test": "echo \"ERROR: tests are not configured for @uixvisor/registry\" && exit 1",
    "typecheck": "echo \"ERROR: typecheck is not configured for @uixvisor/registry\" && exit 1"
  }
}
