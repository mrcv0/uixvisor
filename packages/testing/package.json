{
  "name": "@uixvisor/testing",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Jest/RNTL test utilities and fixtures",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo \"ERROR: @uixvisor/testing is not implemented\" && exit 1",
    "lint": "eslint src",
    "test": "echo \"ERROR: tests are not configured for @uixvisor/testing\" && exit 1",
    "typecheck": "echo \"ERROR: typecheck is not configured for @uixvisor/testing\" && exit 1"
  }
}
