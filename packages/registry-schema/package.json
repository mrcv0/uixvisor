{
  "name": "@uixvisor/registry-schema",
  "version": "0.0.0",
  "private": true,
  "description": "Zod/TS registry item schema and validation",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"TODO: lint registry-schema\" && exit 0",
    "test": "npm run build && node dist/validate-registry.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.0"
  }
}
