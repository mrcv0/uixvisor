{
  "name": "@uixvisor/registry-schema",
  "version": "0.0.0",
  "private": true,
  "description": "Zod/TS registry item schema and validation",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo \"TODO: build registry-schema\" && exit 0",
    "lint": "echo \"TODO: lint registry-schema\" && exit 0",
    "test": "echo \"TODO: test registry-schema\" && exit 0",
    "typecheck": "tsc --noEmit"
  }
}
